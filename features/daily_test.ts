import { follow, daily, day } from "./daily.ts";
import { assertEquals } from "jsr:@std/assert";

Deno.test("follow", async () => {
  // const resp = await follow("20260113");
  // console.log(await resp.text());
  // const resp = await daily("0113");
  // console.log(await resp.text());
  // console.log(await day("/ap260113.html", "/01, 2026"));
});
